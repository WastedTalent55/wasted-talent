---
interface Props {
  href: string;
  variant?: 'primary' | 'secondary';
}
const { href, variant = 'primary' } = Astro.props;
const base = "inline-block px-5 py-3 text-sm font-medium transition-colors duration-150";
const styles = variant === 'primary'
  ? "bg-accent text-black hover:bg-accent-soft"
  : "border border-accent-light/60 text-cream hover:border-accent hover:text-accent";
---
<a href={href} class={`${base} ${styles}`}>
  <slot />
</a>